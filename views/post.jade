extends layout

block content

  link(href='/css/home.css', rel='stylesheet')

  .container
    .row.header
      .col-lg-12
        .thumbnail.text-center
          h1 #{title}
          :markdown
            p #{contents}
          p by #{author}
          a(href="#{author_link}") #{author}

  include footer
